# https://www.chezmoi.io/reference/configuration-file/
# Regenerated on `chezmoi apply`. Use `chezmoi edit-config` for local-only edits
# (prefer a *.local file or age-encrypted data for secrets).

[data]
  # name = "{{ .chezmoi.username }}"

# Example: template-only paths (uncomment and adjust)
# [edit]
#   command = "nvim"
