# Local overrides and secrets (never commit)
*.local
.env
.env.*
.secrets
secrets/
private/

# Chezmoi local config (optional per-machine)
.chezmoi.toml.local
.chezmoiignore.local

# OS / editor noise
.DS_Store
*~
*.swp
*.tmp

# Cursor local project notes (optional)
.cursor/
